import styles from './LogoMain.module.scss'

const LogoMain = () => {
  return (
    <div className={styles.logo}>
      {/*Never Unique*/}
      Some Name
    </div>
  );
};

export default LogoMain;